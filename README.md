# Ruby-CSNS-Java-Package-Cleaner
Ruby script that renames student file directories downloaded from csns to valid package names. Adds them to the java files. Removes existing package declaration if present.
